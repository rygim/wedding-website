<%= form_for(@rsvp) do |f| %>
  <% if @rsvp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rsvp.errors.count, "error") %> prohibited this rsvp from being saved:</h2>

      <ul>
      <% @rsvp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :confirmation_code %><br>
    <%= f.text_field :confirmation_code %>
  </div>
  <div class="field">
    <%= f.label :can_attend %><br>
    <%= f.check_box :can_attend %>
  </div>
  <div class="field">
    <%= f.label :num_attending %><br>
    <%= f.number_field :num_attending %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
